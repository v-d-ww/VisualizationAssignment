# 房价数据可视化分析系统 - 设计理由文档

## 一、整体核心任务

**"地图+多图表联动"的核心任务：精准分析单一/多省份房价全维度特征，识别热点、对比差异、追踪趋势。**

本系统通过3D地图与多个可视化图表的联动，实现以下核心分析目标：

1. **识别房价热点区域**：通过3D地图+热力图，直观展示全国各省份房价分布，快速定位高房价区域
2. **对比多省份房价多维度特征**：通过平行坐标图+雷达图，同时对比多个省份在房价均价、成交量、人均可支配收入、房贷利率、人口密度等维度的差异
3. **下钻分析省份房价层级分布**：通过旭日图，深入分析单一省份的房价构成（地价、建材、税费、利润等）
4. **评估省份房价健康度**：通过雷达图，综合评估省份在房价水平、涨幅、房价收入比、GDP增长率、人口增长率、限购政策强度等维度的健康度
5. **追踪省份间房价关联传导**：通过桑基图，分析省份间的人口流动、经济关联、政策影响等传导机制
6. **预测房价趋势变化**：通过折线图，基于历史数据预测未来房价走势

## 二、联动交互设计理由

### 1. 省份点击联动设计理由

**设计目标**：快速聚焦目标省份，实现多维度特征的同步对比，避免跨页切换的效率损耗。

**设计理由**：

- **效率提升**：用户点击地图上的省份后，所有相关图表（雷达图、平行坐标图、旭日图、折线图、桑基图）立即同步更新，无需手动切换页面或重新选择数据源，大幅提升分析效率
- **上下文保持**：多图表在同一屏幕展示，用户可以同时查看地图位置、多维度对比、趋势预测等多个视角，保持分析的连贯性和完整性
- **对比便利**：支持同时选择最多5个省份，用户可以快速对比不同省份的房价特征，发现差异和规律
- **视觉反馈**：选中的省份在地图上高亮显示（金色描边+半透明白色填充），用户可以清楚知道当前分析的对象

**技术实现**：
- 使用React状态管理（`selectedProvinces`）统一管理选中省份
- 所有图表组件接收`selectedProvinces`作为props，自动响应状态变化
- 使用`useCallback`和`useMemo`优化性能，确保联动更新流畅（≥30fps）

### 2. 时间轴联动设计理由

**设计目标**：动态追踪房价趋势变化，结合多图表验证趋势背后的关联因素（如政策、人口流动）。

**设计理由**：

- **趋势追踪**：用户切换时间轴时，所有图表同步更新到对应时间段的数据，可以直观看到房价随时间的变化趋势
- **关联分析**：通过时间轴联动，用户可以同时观察：
  - 地图热力图：房价空间分布的时间变化
  - 折线图：房价趋势的时间序列
  - 雷达图：多维度特征的时间演变
  - 桑基图：省份间关联的时间变化
- **悬停预览**：时间轴支持悬停预览功能，用户可以在不切换时间的情况下，临时查看某个时间点的数据，方便对比不同时间段
- **数据验证**：通过多图表联动，用户可以验证趋势的可靠性，例如折线图显示上涨趋势时，可以通过雷达图查看是否所有维度都支持这一趋势

**技术实现**：
- 使用`displayYear`和`displayMonth`状态管理当前显示时间
- 支持`previewYear`和`previewMonth`实现悬停预览
- 所有图表组件接收时间参数，自动过滤和展示对应时间段的数据

## 三、任务驱动设计

### 图表任务标签

每个可视化组件都标注了明确的核心任务，帮助用户理解图表的作用：

- **3D地图+热力图**：🔹 核心任务：识别房价热点区域
- **平行坐标图**：🔹 核心任务：对比多省份房价多维度特征
- **旭日图**：🔹 核心任务：下钻分析省份房价层级分布
- **雷达图**：🔹 核心任务：评估省份房价健康度
- **桑基图**：🔹 核心任务：追踪省份间房价关联传导
- **折线图**：🔹 核心任务：预测房价趋势变化

### 动态洞察面板

在界面底部提供可折叠的"动态洞察"面板，自动生成分析结论：

- **自动更新**：当用户选中/取消省份、切换时间轴时，面板自动更新分析结论
- **关联分析**：结论关联当前选中的图表和数据，提供多维度的洞察
- **可折叠设计**：收起时仅显示"📊 查看动态洞察"按钮，不占用过多界面空间

### 交互分析提示

当用户进行关键操作时，显示临时分析提示（3秒后自动消失）：

- **点击省份**：提示用户当前操作对各个图表的影响
- **悬停时间轴**：提示时间切换对数据展示的影响

## 四、技术架构

### 技术栈

- **前端框架**：React + TypeScript
- **可视化库**：d3.js（所有图表均使用d3.js实现，不使用ECharts等其他库）
- **3D渲染**：Three.js（用于3D地图）
- **状态管理**：React Hooks（useState, useCallback, useMemo）

### 性能优化

- 使用`useCallback`缓存回调函数，避免不必要的重渲染
- 使用`useMemo`缓存计算结果，减少重复计算
- 图表组件仅在相关数据变化时重新渲染
- 确保多省份联动时帧率≥30fps，无明显卡顿

### 代码复用

- 优先复用项目中已有的d3.js图表代码
- 仅在无法复用的情况下才重写
- 统一的颜色配置和样式规范

## 五、用户体验设计

### 界面布局

- **一屏展示**：所有核心图表在同一屏幕展示，无需滚动
- **核心突出**：3D地图占据左侧主要区域，突出核心视图
- **辅助图表**：其他图表围绕地图分布，形成清晰的视觉层次

### 交互反馈

- **选中高亮**：选中的省份在地图上高亮显示
- **悬停提示**：鼠标悬停时显示详细数据
- **临时提示**：关键操作时显示分析提示
- **动态洞察**：自动生成分析结论，帮助用户理解数据

## 六、总结

本系统通过"地图+多图表联动"的设计，实现了房价数据的全维度分析。通过省份点击联动和时间轴联动，用户可以高效地进行多维度对比和趋势追踪。通过任务标签、动态洞察面板和交互分析提示，系统显性地体现了"任务驱动"的设计理念，帮助用户更好地理解和使用系统。

