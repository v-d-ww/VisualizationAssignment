# 图表联动机制说明

## 一、当前已实现的连接机制

### 1. **数据联动（核心机制）**

所有图表通过共享状态实现数据联动：

#### 共享状态（在 `MainPage.tsx` 中管理）：
- **`selectedProvinces`**: 选中的省份列表（最多5个）
  - 触发方式：点击地图省份
  - 影响：所有图表同步更新，只显示选中省份的数据
  
- **`displayYear` 和 `displayMonth`**: 当前显示的时间
  - 触发方式：点击时间轴或悬停时间轴
  - 影响：所有图表同步更新到对应时间段的数据

#### 联动流程：
```
用户操作（点击地图/切换时间）
    ↓
MainPage状态更新（selectedProvinces / displayYear）
    ↓
所有图表组件接收新的props
    ↓
图表重新计算数据并渲染
    ↓
所有图表显示相同省份/时间的数据
```

### 2. **视觉联动（部分实现）**

#### 已实现的视觉联动：
1. **平行坐标图 → 地图**
   - 悬停平行坐标图的省份折线时，地图上对应省份会高亮
   - 实现方式：`onProvinceHover` 回调 → `__highlightProvinceOnMap` 全局函数

2. **地图 → 所有图表**
   - 选中省份时，地图高亮显示（金色描边+半透明填充）
   - 所有图表同步显示选中省份的数据

#### 未实现的视觉连接：
- ❌ 图表之间没有视觉连接线
- ❌ 没有数据流可视化
- ❌ 没有明显的"数据传递"视觉反馈

## 二、为什么需要视觉连接？

### 当前问题：
1. **连接不够明显**：用户可能不知道图表之间是联动的
2. **数据流不清晰**：看不出数据是如何在图表间传递的
3. **交互反馈不足**：联动发生时缺少视觉提示

### 视觉连接的价值：
1. **提升理解**：让用户直观看到图表之间的关系
2. **增强交互**：提供清晰的交互反馈
3. **任务驱动**：明确展示分析任务的流程

## 三、如何增强视觉连接？

### 方案1：添加连接线（推荐）

在图表之间添加动态连接线，展示数据流：

```
地图（选中省份）
    ↓ [连接线]
雷达图（显示选中省份的多维度数据）
    ↓ [连接线]
平行坐标图（对比选中省份）
    ↓ [连接线]
折线图（显示选中省份的趋势）
```

**实现方式**：
- 使用SVG绘制连接线
- 连接线在联动时高亮显示
- 添加动画效果（数据流动画）

### 方案2：统一颜色编码

为每个选中的省份分配唯一颜色，在所有图表中使用相同颜色：

- 省份A：蓝色
- 省份B：绿色
- 省份C：橙色
- ...

**实现方式**：
- 创建颜色映射函数
- 所有图表使用相同的颜色方案
- 用户可以通过颜色快速识别同一省份在不同图表中的位置

### 方案3：添加联动提示动画

当联动发生时，显示视觉提示：

- 图表边框闪烁
- 数据点高亮动画
- 连接线流动效果

### 方案4：数据流可视化

在图表之间显示数据流动：

- 从地图流向其他图表的动画
- 数据值在连接线上显示
- 时间变化时的数据流动效果

## 四、推荐实施方案

### 优先级1：统一颜色编码（最简单，效果明显）
- 为每个选中省份分配固定颜色
- 在所有图表中使用相同颜色
- 实现成本低，用户体验提升明显

### 优先级2：添加连接线（视觉效果强）
- 在关键图表之间添加连接线
- 联动时高亮连接线
- 需要设计合理的布局

### 优先级3：联动动画提示（增强反馈）
- 联动发生时显示动画
- 图表边框闪烁
- 数据点高亮

## 五、当前连接机制的优势

1. **数据一致性**：所有图表显示相同的数据源（省份+时间）
2. **实时同步**：状态变化立即反映到所有图表
3. **性能优化**：使用React.memo和useMemo优化渲染

## 六、建议

**立即实施**：统一颜色编码方案
- 快速实现
- 效果明显
- 提升用户体验

**后续优化**：添加连接线和动画
- 需要更多设计工作
- 视觉效果更丰富
- 但可能影响性能

