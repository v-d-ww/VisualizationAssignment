# 项目不符合规则的设计点详细清单

## 一、技术栈要求不符合（严重问题）

### 1.1 大量使用ECharts而非d3.js
**规则要求**：必须使用d3.js，否则分数将不计入

**不符合情况**：
以下10个组件完全使用ECharts进行可视化，未使用d3.js：

1. **RadarChart.tsx** - 雷达图组件
   - 使用 `echarts.init()` 初始化图表
   - 使用 `echarts.EChartsOption` 配置图表
   - 应改为使用d3.js实现雷达图

2. **RingChart.tsx** - 环图组件
   - 使用ECharts的饼图配置
   - 应改为使用d3.js的arc和pie布局实现

3. **PolicyFlowChart.tsx** - 政策流量图组件
   - 使用ECharts的力导向图
   - 应改为使用d3.js的force布局实现

4. **PriceClusterChart.tsx** - 聚类图组件
   - 使用ECharts的散点图
   - 应改为使用d3.js的scatter plot实现

5. **PricePredictionEngine.tsx** - AI预测引擎
   - 使用ECharts的折线图
   - 应改为使用d3.js的line生成器实现

6. **DataSimulator.tsx** - 数据模拟器
   - 使用ECharts的折线图
   - 应改为使用d3.js实现

7. **CalculatorPanel.tsx** - 计算器面板
   - 包含多个ECharts图表（折线图、柱状图、饼图）
   - 应改为使用d3.js实现所有图表

8. **DataStoryBoard.tsx** - 数据故事板
   - 使用ECharts的柱状图
   - 应改为使用d3.js实现

9. **AnomalyDetector.tsx** - 异常检测系统
   - 使用ECharts的堆叠柱状图
   - 应改为使用d3.js实现

10. **InvestmentAdvisor.tsx** - 投资决策助手
    - 使用ECharts的柱状图和折线图
    - 应改为使用d3.js实现

**符合d3.js要求的组件**：
- ✅ TimeAxis.tsx - 时间轴组件（使用d3.js）
- ✅ map3d/drawFunc.ts - 地图投影（使用d3.js的geo投影）

**影响**：这是最严重的问题，根据规则"必须使用d3.js，否则分数将不计入"，项目可能无法获得分数。

---

## 二、任务驱动设计不符合

### 2.1 缺少明确的任务说明
**规则要求**：PP2需要提供2-3个关键任务（如识别热点、对比差异、追踪趋势）

**不符合情况**：
- 项目中没有明确说明要解决的具体任务
- 功能说明清单中列出了功能，但没有明确说明每个视图解决什么任务
- 缺少任务驱动的设计说明

**建议**：
- 明确说明主要任务：如"识别房价热点区域"、"对比不同省份房价差异"、"追踪房价趋势变化"
- 在文档中说明每个可视化组件对应解决的任务

### 2.2 视图与任务对应关系不明确
**规则要求**：需要说明每个视图用于什么任务

**不符合情况**：
- 虽然有多个可视化组件，但没有明确说明：
  - 热力图用于什么任务？
  - 雷达图用于什么任务？
  - 3D散点图用于什么任务？
  - 等等

**建议**：
- 为每个视图明确标注其解决的任务
- 例如：热力图用于"识别房价热点区域"任务

---

## 三、设计理由说明不符合

### 3.1 缺少编码选择说明
**规则要求**：需要说明为什么选择这种编码方式（位置/颜色/大小/形状）

**不符合情况**：
- 没有文档说明为什么选择：
  - 热力图使用颜色编码（为什么用颜色而不是大小？）
  - 雷达图使用位置编码（为什么用雷达图而不是柱状图？）
  - 3D散点图使用位置编码（为什么用3D而不是2D？）
  - 环图使用角度编码（为什么用环图而不是柱状图？）

**建议**：
- 为每个视图添加设计理由说明
- 说明编码选择的依据和优势

### 3.2 缺少颜色映射说明
**规则要求**：需要说明颜色映射、尺度、归一化、聚合粒度

**不符合情况**：
- 热力图：虽然使用了颜色映射，但没有说明：
  - 为什么选择蓝色到红色的渐变？
  - 数据是如何归一化的？
  - 聚合粒度是什么（省份级别？）
- 地图颜色：没有说明颜色分级的依据
- 其他图表：缺少颜色映射的详细说明

**建议**：
- 详细说明每个颜色映射的设计理由
- 说明数据归一化方法
- 说明聚合粒度选择

### 3.3 缺少尺度说明
**规则要求**：需要说明尺度（scale）的使用

**不符合情况**：
- 没有说明坐标轴尺度的选择（线性？对数？）
- 没有说明数据标准化方法
- 雷达图虽然做了归一化，但没有说明为什么这样做

**建议**：
- 说明每个图表的尺度选择
- 说明数据标准化/归一化的方法

---

## 四、任务驱动设计 vs 图表拼凑

### 4.1 可能被判定为"图表拼凑"
**规则要求**：关键评估点是"任务驱动设计"而非"图表拼凑"

**不符合情况**：
- 项目包含大量不同类型的图表（热力图、雷达图、3D散点图、聚类图、词云、环图等）
- 但缺少明确的证据证明这些图表是为了解决特定任务而设计的
- 更像是"把所有可能的图表类型都做出来"

**建议**：
- 明确说明每个图表解决的具体任务
- 说明图表之间的关联性（为什么需要这些图表的组合）
- 说明如何通过图表组合完成分析任务

---

## 五、交互演示不符合

### 5.1 缺少交互演示视频或GIF
**规则要求**：PP4需要系统交互演示视频或GIF，评估交互如：过滤、链接、高亮、刷选、对比、下钻、标注等

**不符合情况**：
- 项目中只有一个demo/3dmap.gif，但这是3D地图的演示
- 缺少完整的系统交互演示
- 缺少展示以下交互的演示：
  - 过滤（如时间轴过滤）
  - 链接（如地图点击联动其他视图）
  - 高亮（如悬停高亮）
  - 刷选（如时间轴刷选）
  - 对比（如多省份对比）
  - 下钻（如地图双击下钻）
  - 标注（如协作标注功能）

**建议**：
- 创建完整的交互演示视频或GIF
- 展示所有关键交互功能
- 展示完整的分析闭环（从问题到洞察）

### 5.2 交互演示可能被判定为"点击操作"
**规则要求**：交互演示不应只是"点击操作"，而应展示"分析闭环"

**不符合情况**：
- 如果只是简单展示点击按钮打开图表，可能被判定为"点击操作"
- 缺少展示完整分析流程的演示

**建议**：
- 展示完整的分析流程：
  1. 提出问题（如"哪些省份房价最高？"）
  2. 使用交互功能（如点击地图、切换时间）
  3. 获得洞察（如发现热点区域）
  4. 进一步分析（如下钻到具体省份）
  5. 得出结论

---

## 六、洞察与证据链不符合

### 6.1 洞察缺少证据链
**规则要求**：洞察需要有证据链，结论要对应到具体的视图/指标

**不符合情况**：
- 虽然有很多可视化组件，但缺少明确的洞察说明
- 缺少说明"如何从视图中得出洞察"
- 缺少说明"洞察对应的具体视图和指标"

**建议**：
- 明确列出关键洞察
- 为每个洞察提供证据链：
  - 使用了哪个视图？
  - 看到了什么数据？
  - 如何得出这个结论？

---

## 七、颜色使用问题

### 7.1 可能存在的颜色误用
**规则要求**：避免颜色误用、误导性尺度、不清晰的图例

**潜在问题**：
- 热力图：需要确认颜色映射是否合理，图例是否清晰
- 地图颜色：需要确认颜色分级是否合理
- 雷达图：需要确认多省份对比时颜色是否足够区分
- 其他图表：需要检查颜色使用是否一致

**建议**：
- 检查所有图表的颜色使用
- 确保图例清晰明确
- 确保颜色映射合理（如低值到高值的渐变方向）

---

## 八、PPT要求不符合

### 8.1 缺少PPT文件
**规则要求**：PPT不超过6页，内容安排：
- PP1：主题、团队成员、分工
- PP2：2-3个关键任务
- PP3：主要视图（一屏显示，无滚动条，不能跨页），说明任务、设计理由、编码选择、颜色映射等
- PP4：系统交互演示视频或GIF

**不符合情况**：
- 项目中没有找到PPT文件（.pptx或.ppt）
- 无法确认PPT是否符合要求

**建议**：
- 创建符合要求的PPT
- 确保PPT不超过6页
- 确保PP3的主要视图能在一屏显示（无滚动条）

---

## 九、原创性要求

### 9.1 无法确认原创性
**规则要求**：必须独立开发设计，发现抄袭将不及格

**检查情况**：
- 无法从代码中直接判断是否原创
- 需要人工审查代码和设计

**建议**：
- 确保所有代码和设计都是原创的
- 如果参考了其他项目，需要明确标注

---

## 十、总结

### 最严重的问题（可能导致无法得分）：
1. **大量使用ECharts而非d3.js** - 10个组件需要重写
2. **缺少PPT文件** - 无法确认是否符合要求

### 重要问题（可能导致扣分）：
3. **缺少任务驱动设计说明** - 需要明确任务和视图对应关系
4. **缺少设计理由说明** - 需要说明编码选择、颜色映射等
5. **缺少完整的交互演示** - 需要创建演示视频/GIF
6. **可能被判定为"图表拼凑"** - 需要证明是任务驱动设计

### 需要改进的问题：
7. **洞察缺少证据链** - 需要明确洞察与视图的对应关系
8. **颜色使用需要检查** - 确保颜色映射合理、图例清晰

---

## 建议的改进方案

### 优先级1（必须完成）：
1. 将所有ECharts图表改为d3.js实现
2. 创建符合要求的PPT（6页以内）

### 优先级2（重要）：
3. 明确任务驱动设计：
   - 列出2-3个关键任务
   - 说明每个视图解决的任务
4. 添加设计理由说明：
   - 编码选择理由
   - 颜色映射说明
   - 尺度选择说明
5. 创建完整的交互演示视频/GIF

### 优先级3（改进）：
6. 建立洞察证据链
7. 检查并优化颜色使用
8. 确保原创性

